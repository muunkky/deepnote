Use `.venv/Scripts/python.exe` to run Python commands.

The code for the gitban card with id 321p72 has been approved as of commit 778b05b. Please use the gitban tools to update the gitban card and begin the tasks required to properly complete it.

## Card Close-out tasks:
- Use gitban's checkbox tools to ensure all checkboxes on the card are checked off for completed work if not already.
- Do not mark any work as deferred. This card will be closed and archived and likely never seen again.
- Use gitban's complete card tool to submit and validate if not already completed.
- Close-out items: none outstanding from the review. The reviewer confirmed the card is correct, the capstone was independently reproduced (182/182 from both repo-root and `packages/cli` CWDs), and all project quality gates pass on the changed file. The one non-blocking follow-up (L1 guard-granularity: enumerate individual fixture files in the module-load guard rather than only the two base directories) has been routed to the planner as a sprint card — do NOT implement it here.

Note: You are closing out this card only. The dispatcher owns sprint lifecycle — do not close, archive, or finalize the sprint itself. The exception is a sprint close-out card, which will be obvious from its content.
