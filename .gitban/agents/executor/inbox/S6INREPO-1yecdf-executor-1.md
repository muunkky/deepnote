Use `.venv/Scripts/python.exe` to run Python commands.

The code for the gitban card with id 1yecdf has been approved as of commit 7c0f292. Please use the gitban tools to update the gitban card and begin the tasks required to properly complete it.

## Card Close-out tasks:

- Use gitban's checkbox tools to ensure all checkboxes on the card are checked off for completed work if not already.
- Do not mark any work as deferred. This card will be closed and archived and likely never seen again.
- Use gitban's complete card tool to submit and validate if not already completed.
- Close-out items: None. The reviewer recorded "Outstanding close-out actions: None blocking." The two follow-up items (L1 tool-wiring-coverage-gap, L2 mcp-merge-coverage-gap) are NOT close-out items — they require new test coverage and re-running the suite, so they are being routed to the planner for sprint capture. Do not attempt them here.
- The PR agent owns cutting the clean `feat/*` branch off `upstream/main`. The executor correctly did NOT do this on the worktree — do not cut it now.

Note: You are closing out this card only. The dispatcher owns sprint lifecycle — do not close, archive, or finalize the sprint itself. This card is part of sprint S6INREPO and is NOT the sprint close-out card (that is o5pg2k), so do not finalize the sprint.
