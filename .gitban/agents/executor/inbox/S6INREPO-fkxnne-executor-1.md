Use `.venv/Scripts/python.exe` to run Python commands.

The code for the gitban card with id fkxnne has been approved as of commit 2b3a4dd26c12a861c34c965571d8f18ca7f2c43c. Please use the gitban tools to update the gitban card and begin the tasks required to properly complete it.

## Card Close-out tasks:

- Use gitban's checkbox tools to ensure all checkboxes on the card are checked off for completed work if not already.
- Do not mark any work as deferred. This card will be closed and archived and likely never seen again.
- Use gitban's complete card tool to submit and validate if not already completed.
- Close-out items: none. The reviewer raised no blockers and no follow-up items. The card's own follow-up notes (apply the binding-identity assertion pattern to other executor paths that replay recorded fixtures rather than invoking registered callbacks) were judged documentation-grade by the reviewer and do NOT need a new card — leave them as notes on the card.
- If this card is not in a sprint, push the feature branch and create a draft PR to main using `gh pr create --draft`. Do not merge it — the user reviews and merges.

Note: You are closing out this card only. The dispatcher owns sprint lifecycle — do not close, archive, or finalize the sprint itself. The exception is a sprint close-out card, which will be obvious from its content.
