Use `.venv/Scripts/python.exe` to run Python commands.

The code for the gitban card with id pv4px0 has been approved as of commit de31e4a. Please use the gitban tools to update the gitban card and begin the tasks required to properly complete it.

## Card Close-out tasks:

- Use gitban's checkbox tools to ensure all checkboxes on the card are checked off for completed work if not already. The "Code Review Approved" checkbox (Feature Work Phases table) is now true — the reviewer approved at commit de31e4a — so flip it.
- Do not mark any work as deferred. This card will be closed and archived and likely never seen again.
- Use gitban's complete card tool to submit and validate if not already completed.
- Close-out items: none. The reviewer raised a single non-blocking follow-up (L1, the ADR-001 bare-system-python hint) which is explicitly out of scope for pv4px0 ("Scope decided; do not re-architect") and has been routed to the planner — do NOT implement it on this card.
- This card is in sprint S6INREPO, so do NOT create a PR. The dispatcher owns sprint-level PR/deploy.

Note: You are closing out this card only. The dispatcher owns sprint lifecycle — do not close, archive, or finalize the sprint itself. This is not a sprint close-out card (the closeout card is o5pg2k).
