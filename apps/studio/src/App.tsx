// Trivial root component — the smoke target for the React + Vite + jsdom + RTL
// pipeline (LUIVIEW1 step 2). Replaced by the real app shell in step 3.
export function App() {
  return <h1>Deepnote Studio</h1>
}
